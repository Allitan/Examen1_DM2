import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Examen4Page } from './examen4.page';

describe('Examen4Page', () => {
  let component: Examen4Page;
  let fixture: ComponentFixture<Examen4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Examen4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
